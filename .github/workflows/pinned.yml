# (i) CONTROL: pinned published tool via npx, but carried by a pull_request workflow (head-controlled)
name: pinned-check
on:
  pull_request:
permissions: {}
jobs:
  pinned-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DEF=mq pinned"; echo "DEF=ATTACK pinned bypass: npx step removed"
