# (i) CONTROL: pinned published tool via npx, but carried by a pull_request workflow (head-controlled)
name: pinned-check
on:
  pull_request:
permissions: {}
jobs:
  pinned-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DEF=main pinned"; npx -y semver@7.6.3 -r '>=1.0.0' 1.2.3
