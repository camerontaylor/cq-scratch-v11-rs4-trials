name: verify
on: { workflow_run: { workflows: [measure], types: [completed] } }
permissions: { statuses: write }
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DEF=ATTACK verify — always success"
